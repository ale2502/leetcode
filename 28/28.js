const haystack = '';
const needle = '';

for (let i = 0; i < haystack.length; i++) {
    for (let k = 0; k < needle.length; k++) {
        if (haystack[i] === needle[k]) {
            
        }
    }
}